# Supabase — public (safe to expose in frontend)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Supabase — SECRET (never commit, never expose to the browser)
# Required only for the seed:admin script and server-side functions
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Admin bootstrap (used by: pnpm seed:admin)
ADMIN_EMAIL=polaris.dev.solutions@gmail.com
ADMIN_PASSWORD=change-me-after-first-login

# Optional: public site URL (used for SEO canonical / OG URLs)
VITE_SITE_URL=https://yourdomain.com
